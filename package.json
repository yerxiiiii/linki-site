{
  "name": "linki-cangcang-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js",
    "local": "ADMIN_PUBLIC=1 ADMIN_USER=local ADMIN_PASSWORD=local node --env-file=.env --watch server.js"
  }
}
